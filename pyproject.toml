[project]
name = "edupersona"
version = "0.2.3"
description = "Bridging the gap between eduID and IAM systems in education"
readme = "README.md"
requires-python = ">=3.12"
license = "AGPL"
authors = [
    { name = "Peter Kleynjan" },
]

[tool.pyright]
extraPaths = ["."]
pythonVersion = "3.12"
